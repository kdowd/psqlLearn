# <Topic> — <bounded scope>

> Scaffold for the `learning-designer` skill. Replace every `<...>` and delete guidance
> comments. Keep the section order; drop a section only if it is genuinely inapplicable.

## Assumptions

Only if defaults were needed: `<audience / time budget / environment / dataset>`.

## Objectives

By the end you will be able to:

1. `<observable verb> <artifact produced>`
2. `<...>`
3. `<...>`

## Setup

```bash
<exact commands that create or load the data>
```

```sql
-- confirm the environment works; expected: <N> rows, <columns>
SELECT ...
```

## Worked example

**Goal:** `<one sentence>`

```sql
<complete, runnable solution>
```

**Why this works, step by step:**

1. `<clause / function>` does `<what>` because `<reason>`.
2. `<...>`

Expected result: `<N>` rows, columns `<a, b, c>`.

## Guided exercise

**Goal:** `<goal>` — structure supplied, you fill the gaps.

```sql
SELECT <fill in>
FROM   contacts
WHERE  <fill in>;
```

Expected result: `<N>` rows.

## Exercises

### Exercise 1 — `<title>`

`<problem statement>`

- **Expected:** `<N>` rows, columns `<a, b>`
- **Check:**
  ```sql
  <query that confirms correctness>
  ```

<details><summary>Hints</summary>

1. **Restate.** `<re-express the goal in concept vocabulary>`
2. **Point.** `<name the clause / function / operation involved>`
3. **Edge.** `<name the trap, short of a complete answer>`

</details>

### Exercise 2 — `<title>`

`<same shape as Exercise 1>`

### Exercise 3 — `<title>`

`<same shape as Exercise 1>`

## Solutions

### Exercise 1

```sql
<runnable solution>
```

`<one or two sentences on why, and the near-miss it avoids>`

### Exercise 2

```sql
<runnable solution>
```

### Exercise 3

```sql
<runnable solution>
```

## Misconceptions

- **"`<wrong model>`"** — tempting because `<reason>`.
  ```sql
  <smallest query that disproves it>
  ```

## Transfer task

Same structure, different story: `<restated problem in an unfamiliar domain>`. No new syntax
is required — recognize the pattern, not the answer.
